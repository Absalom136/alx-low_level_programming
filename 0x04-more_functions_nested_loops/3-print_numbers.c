#include "main.h"

/**
 * print_numbers - a function that prints 0 - 9
 * Return: Always 0
 */

void print_numbers(void)
{
	int y;

	for  (y = 0; y <= 9; y++)
	{
		_putchar((n % 10) + '0);
	}
	_putchar('\n');
}
